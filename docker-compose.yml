version: '3.8'
services:
  ecomos-dev:
    build: .
    volumes:
      - .:/kernel
    ports:
      - "1234:1234"  # GDB调试端口
      - "5900:5900"  # VNC端口
    stdin_open: true
    tty: true
    working_dir: /kernel